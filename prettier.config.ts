import { type Config } from 'prettier';

const config: Config = {
  tabWidth: 2,
  useTabs: false,
  semi: true,
  singleQuote: true,
  printWidth: 80,
  trailingComma: 'none'
};

export default config;
